embedder:
    azure:
        model: ""
        endpoint: ""
        api_key: ""
        api_version: "2024-12-01-preview"

vector_db:
    weaviate:
        url: "local"
        collection_name: "document"
    mongodb:
        uri: "mongodb://your_mongo_uri"
        db_name: "your_db_name"
        collection_name: "your_collection_name"

chunker:
    fixed:
        overlap: 20
        chunk_size: 250
    semantic: {}  # No additional config needed for semantic mode

chat_completion:
    azure:
        model: "chat_model"
        api_key: "your_api_key"
        endpoint: "https://chat-api.com"
